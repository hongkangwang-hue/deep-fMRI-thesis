# 硕士论文代码核查与结果补充清单

> 目标：只补齐完成硕士论文正式稿所必需的数据，不增加新被试、新数据集、新模型或额外实验。

> **执行状态（2026-07-13）**：本清单第 1/2/4 节 + 第 3/5/7/9(mask部分) 节的大部分已用
> 真实数据核对完成，产出在 `thesis_supplement/`（脚本在 `scripts/thesis_supplement_*.py`，
> 均可重跑核对）。第 6/8/9(shift数值)/10 节需要的 `results/m5_stats/`、
> `results/m4_full_matrix/` 等文件当前只在 AutoDL 服务器上（本次会话 SSH 连接
> 被拒，端口可能已换，需要你确认服务器状态/新端口后我再同步）；第 10 节的
> PCA evr@100 与 English1000 的逐 ROI 相关系数是两处目前公认的真实缺口，详见
> 各节末尾说明。逐节状态见下方每节标题旁的批注。

## 1. 故事与交叉验证

需要确认并导出：

- 最终用于分析的独立故事总数：83 还是 84
- 是否存在重复故事
- 重复故事是否排除在 outer CV 之外
- 三折故事数是否为 28 / 28 / 27
- 每折有效评分 TR 数

建议文件：`story_manifest.csv`

至少包含：

- `story_id`
- `fold_id`
- `duration_sec`
- `n_words`
- `n_tr_raw`
- `n_tr_scored`
- `is_repeated`
- `included_in_cv`
- `exclusion_reason`

---

## 2. `eligible_h128` 目标词集合

需要提供：

- 全部故事原始词数
- `eligible_h128` 总数
- 每折 `eligible_h128` 数量
- H=8、32、128 是否使用完全相同的目标词集合
- 每个故事首个合格目标词的 midpoint
- 首个合格目标词时间的最大值和中位数
- 缺失时间标注词数
- 非有限特征数量

建议文件：`target_audit.csv`

至少包含：

- `story_id`
- `fold_id`
- `n_words_raw`
- `n_eligible_h128`
- `first_eligible_midpoint_sec`
- `n_missing_time_labels`
- `n_invalid_features`

---

## 3. 模型、Tokenizer 与层位

四个模型均需核对：

- Pythia-160M
- RWKV-4-Pile-169M
- Mamba-130M
- AWD-LSTM WT103

需要提供：

- 完整 `model_id`
- `revision` 或 commit
- 精确参数量
- 总层数
- hidden size
- tokenizer 名称与词表大小
- 主分析层
- 最终层
- 代码中的实际 hidden-state 索引
- 每个目标窗口的状态重置方式
- AWD-LSTM 的 `xxunk` 数量与比例

建议文件：`model_metadata.csv`

---

## 4. 实际执行的预处理流程

请根据最终代码确认，不要照计划书推测：

- 是否排除故事开始前 100 秒
- 是否排除故事结尾 20 秒
- 是否使用 Savitzky–Golay 滤波
- 是否对 BOLD 做标准化
- 标准化是否只基于训练故事
- 词时间是否使用 midpoint
- Lanczos 重采样参数
- FIR 是否为 2、4、6、8 秒
- PCA 是否在 FIR 之前
- PCA 是否只使用 outer-training stories 拟合
- Ridge λ 候选范围
- λ 是否在训练故事内部选择

建议整理为：`pipeline_config.txt`

---

## 5. IFG 主层完整结果

每名被试、每个模型都需要：

- `r8`
- `r32`
- `r128`
- 三者的 95% CI
- `delta_local = r32 - r8`
- `delta_long = r128 - r32`
- `delta_total = r128 - r8`
- 三种增益的 95% CI

覆盖：

- UTS01、UTS02、UTS03
- Pythia、RWKV、Mamba、AWD-LSTM

建议文件：`roi_results_full.csv`

---

## 6. 两项核心架构差值

每名被试需要提供：

- `RWKV_delta_total - Pythia_delta_total`
- `Mamba_delta_total - Pythia_delta_total`

每项包括：

- 点估计
- 95% CI
- 原始 p 值
- Holm 校正后的 p 值
- 是否拒绝零假设

建议文件：`contrasts_bootstrap.csv`

---

## 7. PT 完整结果

每名被试、每个核心模型至少需要：

- `r8`
- `r32`
- `r128`
- `delta_local`
- `delta_long`
- `delta_total`
- `delta_total` 的 95% CI

AWD-LSTM 的 PT 结果可放附录。

---

## 8. 最终层稳健性结果

每名被试需要：

- 最终层 `r8`
- 最终层 `r32`
- 最终层 `r128`
- 最终层 `delta_total`
- 最终层 RWKV−Pythia
- 最终层 Mamba−Pythia
- 两项差值的 95% CI
- 与主层方向是否一致
- 是否发生实质性翻转

---

## 9. 40 秒 time-shift 结果

每名被试、每个核心模型需要：

- shift `r8`
- shift `r32`
- shift `r128`
- shift `delta_total`

另外需要：

- shift 条件下 RWKV−Pythia
- shift 条件下 Mamba−Pythia
- 对应 95% CI
- normal 与 shift 的评分 mask 是否逐元素一致
- 所有故事的 `n_mask_difference` 是否为 0

建议文件：

- `shift_results.csv`
- `mask_audit.csv`

---

## 10. PCA、Ridge 与 English1000 审计摘要

### PCA

需要：

- 每个模型、H、fold 的 `evr@100`
- 每个模型的均值或范围

### Ridge

需要：

- λ 中位数
- λ 四分位范围
- 命中下界比例
- 命中上界比例

### English1000

需要补齐：

- UTS01 全体素相关
- UTS02 全体素相关
- UTS03 全体素相关
- 三名被试 IFG 相关
- 三名被试 PT 相关
- 原始判据是否通过
- ROI 补充判据是否通过
- 未通过原因

建议文件：

- `pca_ridge_audit.csv`
- `english1000_validation.csv`

---

# 最终完成标准

只需确认以下内容均已获得：

- [x] 故事总数与三折划分一致——`thesis_supplement/story_manifest.csv`：84 故事（respdict
      权威计数），83 个用于 3 折 CV（28/28/27），`wheretheressmoke` 是唯一的重复/held-out
      探针故事，排除在 CV 外。
- [x] `eligible_h128` 数量已确定——`thesis_supplement/target_audit.csv`：全部故事原始词数
      158598，`eligible_h128` 总数 147846（与 [[m0-three-subject-extension]] 记录的哈希
      来源一致）；H=8/32/128 共用同一目标词集合（schema 只有一个 `eligible_h128` 列，
      设计上按最严格的 H=128 阈值统一定义）。
- [x] 四个模型版本、tokenizer 和层索引已确定——`thesis_supplement/model_metadata.csv`。
      **重要发现**：`config.yaml` 里 rwkv/mamba 声明的 model_id 与代码实际运行时用的不同
      （rwkv: `BlinkDL/rwkv-4-pile-169m`→实际`RWKV/rwkv-4-169m-pile`；mamba:
      `state-spaces/mamba-130m`→实际`state-spaces/mamba-130m-hf`），论文 Methods 需要写
      实际用的 id，不能照抄 config.yaml。pythia/mamba 精确参数量已从 HF safetensors 元数据
      精确读到（162,323,040 / 129,135,360）；RWKV 精确参数量、AWD-LSTM 精确参数量+词表大小
      需服务器跑 `scripts/thesis_supplement_server_model_audit.py`（已写好，等你确认）。
- [ ] AWD-LSTM `xxunk` 比例已确定——**未完成**，需服务器跑上面同一个脚本（本机没有
      fastai/torch，无法本地解出 WT103 词表）。
- [x] IFG 主层完整结果和 95% CI 已导出——`thesis_supplement/roi_results_full.csv`（重排自
      服务器已算好、已同步本地的 `figures/<subject>/tables/table2_full_numbers.csv`，
      3被试×4模型×2ROI）。
- [ ] 两项核心架构差值及 Holm 结果已导出——脚本已写好
      (`scripts/thesis_supplement_m5_derived.py` → `contrasts_bootstrap.csv`)，但依赖的
      `results/m5_stats/<subject>/m5_results.json` 本机没有（只在服务器），本次会话 SSH
      连接不上，**待你确认服务器状态后同步**。
- [x] PT 完整结果已导出——同 IFG，已含在 `roi_results_full.csv` 里（`roi` 列区分
      `left_IFG`/`bilateral_PT`）。
- [ ] 最终层结果已导出——脚本已写好 (`scripts/thesis_supplement_m5_derived.py` →
      `final_layer_robustness.csv`)，同样待 `m5_results.json` 同步。
- [x]/[ ] 40 秒 shift 结果与 mask 一致性已核查——**mask 一致性部分已完成**
      （`thesis_supplement/mask_audit.csv`：3被试×83故事全部逐元素一致，
      `n_mask_difference`全部为0；mask只依赖故事时长，本机纯CPU可独立验证，不需要服务器）。
      **shift 的 r 值/CI 部分未完成**，脚本已写好 (→`shift_results.csv`)，待
      `m5_results.json` 同步。
- [ ] PCA、Ridge 和 English1000 审计摘要已补齐——**部分完成**：English1000 的全体素/
      voxel_mask限定相关系数 + 原始判据/补充判据通过情况已从
      `milestone/工作留痕_三被试扩展_全部M.md` 的真实记录整理进
      `thesis_supplement/english1000_validation.csv`（UTS01/UTS02 原始判据 FAIL、走第8条
      补充判据通过；UTS03 原始判据直接 PASS）。**两处确认的真实缺口**：①逐 ROI
      （IFG/PT）相关系数从未算过，脚本已写好待服务器 npz 文件；②PCA evr@100 从未持久化到
      任何结果文件（`m4_driver.py` 算了但没存盘），Ridge λ 分布本身脚本已写好
      (`scripts/thesis_supplement_pca_ridge_audit.py`)但待 `results/m4_full_matrix/` 同步。
- [x] 论文所写预处理步骤与最终代码完全一致——`thesis_supplement/pipeline_config.txt`
      逐条核对代码实现，含一处需要论文措辞更正的点："排除结尾20秒"在代码里实际是
      trim_last=5个TR(10秒)的编码衔接规则，不是独立的20秒规则。

以上内容齐全后，已经足以完成硕士论文正式稿。**当前进度**：本机可独立完成的部分
（1/2/4节、9节mask部分、3节model_id发现、10节English1000历史记录部分）已全部产出
真实数据；剩余缺口全部是"服务器已有数据待同步"或"需要你确认后再跑的小型补算"，
不是无法完成，见各节脚本与上面逐项说明。
