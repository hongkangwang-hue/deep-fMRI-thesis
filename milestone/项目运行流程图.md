# 项目运行流程图

> 本文档只描述**项目实际是怎么跑的**（脚本、数据流、算力位置），不引入任何新设计。
> 所有节点名对应仓库里真实存在的脚本/目录/常量。

---

## 图 1 — 总览：一次完整实验从数据到论文图表

```mermaid
flowchart TD
    subgraph IN["输入数据（LeBel et al. 公开数据集）"]
        A1["刺激 TextGrid<br/>词 + onset/offset"]
        A2["BOLD 响应 .hf5<br/>逐被试 × 逐故事"]
        A3["respdict.json<br/>每故事 TR 行数"]
        A4["皮层表面 + aparc.a2009s<br/>annot / pycortex transform"]
    end

    subgraph M0["M0 冻结地基（本机·零算力）"]
        B["scripts/m0_build_frozen.py"]
        B1["frozen/story_manifest.csv<br/>frozen/word_index.parquet<br/>frozen/fold_split.json<br/>frozen/analysis_spec.yaml<br/>frozen/contrast_registry.yaml<br/>frozen/roi_spec.json"]
    end

    subgraph M2["M2 fMRI 空间准备（本机·轻量）"]
        C1["m2_validate_pipeline.py --step voxel_mask<br/>BOLD-only 体素筛选"]
        C2["m2_validate_pipeline.py --step roi<br/>annot → BOLD 列（主导顶点规则 B）"]
        C3["frozen/voxel_mask_被试.npy<br/>frozen/roi_columns_被试.npz"]
    end

    subgraph M2C["M2-C 管线外部锚定（服务器 ridge）"]
        D1["m2c_phase1_gpu.py / m2c_phase1_native.py<br/>用 English1000 词嵌入跑同一管线"]
        D2["m2c_compare.py<br/>对齐已发表参考 corrs"]
        D3["受限相关 ≈ 0.995<br/>→ 管线实现无误"]
    end

    subgraph M1["M1 特征提取（服务器·GPU）"]
        E["m1_extract_features.py"]
        E1["cache/features/ 模型/故事/H.npz<br/>主层 + 最终层向量"]
    end

    subgraph M3["M3 竖切验证（服务器）"]
        F1["m3a_blind_check.py<br/>盲态：只看 λ 边界/PCA/NaN，不看 held-out r"]
        F2["m3_vertical_slice.py<br/>1 模型 × 1 H 端到端跑通"]
        F3["m3b_dual_path.py<br/>Pythia+Mamba 首次正式 held-out r + 40s 负控制"]
    end

    subgraph M4["M4 全矩阵拟合（服务器·最重）"]
        G1["m4_pythia.py"]
        G2["m4_mamba.py"]
        G3["m4_rwkv.py"]
        G4["m4_awd_lstm.py"]
        G5["m4_aggregate.py<br/>只扫已有结果，不算"]
        G6["results/m4_full_matrix/ 被试/cells/ 逐单元 json<br/>逐 story 的 ROI z + voxel r"]
    end

    subgraph M5["M5 统计（本机或服务器·纯 CPU）"]
        H1["m5_analysis.py<br/>配对 story bootstrap ×1000<br/>确认性家族 Holm 校正"]
        H2["m5_cross_subject.py<br/>方向一致性判读（不池化）"]
        H3["results/m5_stats/ 被试/m5_results.json"]
    end

    subgraph M6["M6 交付（本机·纯绘图）"]
        I1["m6_figures_v2.py<br/>Figure 2-5 逐被试"]
        I2["m6_cross_subject_figures.py<br/>三被试并排"]
        I3["m6_figures.py fig1/fig6<br/>m6_tables.py / m6_roi_location.py<br/>Figure 1 管线图 + Table 1-2 + QC 表"]
        I4["figures/ 被试/v2/ 图片<br/>figures/ 被试/tables/ 表格"]
    end

    A1 --> B
    A3 --> B
    B --> B1
    A2 --> C1
    A4 --> C2
    C1 --> C3
    C2 --> C3
    B1 --> C1

    B1 --> E
    A1 --> E
    E --> E1

    E1 --> D1
    C3 --> D1
    D1 --> D2 --> D3

    E1 --> F1 --> F2 --> F3
    C3 --> F2
    B1 --> F2

    E1 --> G1 & G2 & G3 & G4
    C3 --> G1 & G2 & G3 & G4
    B1 --> G1 & G2 & G3 & G4
    A2 --> G1 & G2 & G3 & G4
    G1 & G2 & G3 & G4 --> G6 --> G5
    F3 -.先跑通再放大.-> G1

    G6 --> H1 --> H3 --> H2
    B1 --> H1
    H3 --> I1 & I2 & I3
    G6 --> I3
    C3 --> I3
    I1 & I2 & I3 --> I4
```

**关键点**

- `frozen/` 是全流程的**唯一真相源**：故事清单、词索引、三折划分、ROI 规则、分析参数在 M0/M2 一次性冻结，之后任何一步都只读不改。
- M2-C 是一条**旁路验证**：拿 English1000 走完全相同的 assemble→PCA→FIR→ridge，与已发表结果对齐到 0.995，用来证明"管线本身没写错"，然后才敢相信 M4 的模型比较结果。
- M3 是"先竖切再铺开"：M3a 盲态查参数 → M3 单模型端到端 → M3b 双模型 + 负控制，全部通过后才展开 M4 全矩阵。
- M5/M6 **不重跑任何模型推理或 ridge**，只读 M4 落盘的逐 story 结果。

---

## 图 2 — 单个矩阵单元内部：一次 `run_fold` 到底做了什么

这是整个项目的计算核心（[src/ridge/pipeline.py](../src/ridge/pipeline.py) 的 `run_fold`）。

```mermaid
flowchart TD
    S0["单元 = (subject, model, H, layer, fold, 条件)"]

    subgraph ASM["1. 组装 assemble_story（逐故事）"]
        P1["读特征缓存<br/>cache/features/ 模型/故事/H.npz"]
        P2["词中心时刻 = (onset+offset)/2<br/>按时间排序（Lanczos 要求单调）"]
        P3["Lanczos 重采样 word_to_tr<br/>词级向量 → TR 级 X"]
        P4["裁边 X[10:-5]<br/>（pad 5 + trim 10 / trim 5）"]
        P5["读 BOLD Y<br/>只取 voxel_mask 保留列"]
        P6["StoryData(X, Y, tr_times)<br/>三者行数必须相等，否则报错"]
    end

    subgraph FIT["2. 训练折拟合（防泄漏：只用训练故事）"]
        Q1["训练故事 X 纵向拼接"]
        Q2["StandardScaler.fit"]
        Q3["PCA.fit → 100 维<br/>（evr 仅作诊断，不参与拟合）"]
        Q4["transform → FIR 延迟 (2,4,6,8)s<br/>100 × 4 = 400 列"]
        Q5["只保留 FIR-valid 行"]
        Q6["himalaya RidgeCV<br/>λ ∈ logspace(-2,7,19)，inner 2 折"]
    end

    subgraph TEST["3. 测试折预测与评分"]
        R1["测试故事：用训练折的 scaler/PCA<br/>transform → FIR"]
        R2["一次 predict（不按故事拆）"]
        R3["逐故事构建评分 mask：<br/>FIR-valid ∩ 故事开头超过 100s<br/>负控制时再交 shift-valid"]
        R4["每 story：逐体素 Pearson r"]
        R5["每 story × 每 ROI：<br/>fisher-z → ROI 内均值 → 一个标量"]
        R6["fold 级汇总：按有效 TR 数加权<br/>voxel 在 r 空间 / ROI 在 z 空间"]
    end

    OUT["落盘 results/m4_full_matrix/ 被试/cells/ 单元 json<br/>含全部 per-story 分数（= M5 的 bootstrap 抽样单位）"]

    S0 --> P1 --> P2 --> P3 --> P4 --> P6
    P5 --> P6
    P6 --> Q1 --> Q2 --> Q3 --> Q4 --> Q5 --> Q6
    P6 --> R1
    Q2 -.复用参数.-> R1
    Q3 -.复用参数.-> R1
    Q6 -.复用权重.-> R2
    R1 --> R2 --> R3 --> R4 --> R5 --> R6 --> OUT
```

**三条不可违反的纪律都在这张图里**

1. **无泄漏**：scaler / PCA / λ 全部只在该折的训练故事上 fit，测试故事只 transform。
2. **story 级评分**：拟合和预测是整折一次完成的，只有**评分**按故事切片——因为 M5 的 bootstrap 单位是 story，必须保留 per-story 分数。
3. **ROI 聚合顺序固定**：在 story 级完成 fisher-z→ROI 均值，绝不从 fold 级 voxel r 反推（两种加权顺序不等价）。

---

## 图 3 — M4 矩阵怎么展开的（规模来源）

```mermaid
flowchart LR
    subgraph DIM["矩阵维度"]
        D1["被试 × 3<br/>UTS01 / UTS02 / UTS03<br/>逐被试建模，不跨被试池化"]
        D2["模型 × 4<br/>Pythia-160M / Mamba-130M<br/>RWKV-4-169M / AWD-LSTM"]
        D3["上下文长度 H × 3<br/>8 / 32 / 128 原始词"]
        D4["外折 × 3<br/>frozen/fold_split.json"]
    end

    subgraph LAY["层位 × 条件"]
        L1["主层：正常 + 40s shift<br/>× 2 ROI（左IFG + 双侧PT）"]
        L2["最终层：仅正常<br/>× 左IFG（稳健性）"]
    end

    subgraph CNT["单模型单被试 run_fold 次数"]
        N1["主层 3H × 3折 × 2条件 = 18"]
        N2["最终层 3H × 3折 × 1条件 = 9"]
        N3["合计 27 次"]
    end

    T["每被试 4 模型 = 108 次 run_fold<br/>4 个独立进程并行，各自断点续跑<br/>（--skip-existing 按单元跳过）"]

    D1 --> D2 --> D3 --> D4 --> L1 & L2
    L1 --> N1
    L2 --> N2
    N1 & N2 --> N3 --> T
```

`m4_pythia.py` / `m4_mamba.py` / `m4_rwkv.py` / `m4_awd_lstm.py` 是四个**独立入口**，共享
[src/ridge/m4_driver.py](../src/ridge/m4_driver.py)。拆成四个进程是为了能用不同 GPU / 终端分别启动、
互不阻塞、单独重跑；`m4_aggregate.py` 只扫盘不算，可在任何时候查进度（缺失单元会列在 manifest 里）。

---

## 图 4 — 统计口径分层（M5 最容易搞错的地方）

```mermaid
flowchart TD
    IN["M4 逐 story 结果"]

    BS["配对 story bootstrap ×1000<br/>seed 20260701<br/>成对条件复用同一批 story 索引"]

    subgraph CONF["确认性家族（唯一做多重校正的）"]
        C1["Δr_total: RWKV − Pythia"]
        C2["Δr_total: Mamba − Pythia"]
        C3["双尾 bootstrap p → 逐被试 Holm 校正"]
    end

    subgraph EXP["探索性（只报点估计 + 95%CI，不校正）"]
        X1["分段增益 Δr_local / Δr_long"]
        X2["PT ROI 全部结果"]
        X3["RQ1 H-specific 差值"]
        X4["最终层 vs 主层翻转判定"]
    end

    subgraph DIAG["诊断性（不做推断声明）"]
        Y1["40s time-shift 负控制"]
        Y2["λ 边界命中率 / PCA evr / NaN"]
        Y3["M4S 全部产出（见图 5）"]
    end

    CS["m5_cross_subject.py<br/>方向一致 / 部分一致 / 不一致<br/>纯描述性，不合并成人群样本"]

    IN --> BS --> CONF & EXP & DIAG
    CONF --> CS
    EXP --> CS
```

**红线**：三名被试**从不池化**，不产出任何组水平 CI。跨被试的说服力来自"同一效应在三个人身上独立重现"，
而不是合并后的显著性。

---

## 图 5 — 补充实验支线 M4S：刺激侧上下文控制（C1）

导师建议的控制实验（[实验补充/](../实验补充/)），检验 Context Gain 是否真依赖语言历史，
而非只是"输入更长/特征更平滑/故事位置效应"。

```mermaid
flowchart TD
    subgraph PERM["Step 1-2 扰动构造（本机·零算力）"]
        K1["实验补充/src/context_perturb.py<br/>seed 由 master_seed 与 story_id 经 sha256 导出"]
        K2["每故事一个固定置换<br/>再在置换后的序列上滑窗"]
        K3["m4s_assert_impl.py 断言<br/>84/84 故事确定性<br/>443,286 组相邻窗重叠 0 失败"]
        K4["⚠️ 关键：绝不逐窗独立打乱<br/>否则 H−1 重叠被破坏，r 落差无法解释"]
    end

    subgraph EXT["Step 3 扰动特征提取（服务器·GPU ~1h）"]
        M1["m4s_extract_perturbed_features.py<br/>复用 base.py 的 window_override"]
        M2["cache/features_ctx1/ 独立目录<br/>normal 缓存完全不动"]
    end

    subgraph RID["Step 4 拟合（服务器·每被试 ~55min）"]
        N1["m4s_ridge_perturbed.py<br/>C1 × 3模型 × H=8/128 × 3折<br/>× 主层 × 左IFG × 3被试 = 54 单元"]
        N2["目标词/时间戳/BOLD/mask/ROI/折 全部不变"]
    end

    subgraph STA["Step 5 估计量（CPU）"]
        O1["m5s_context_control_stats.py<br/>normal 与 ctx1 装进同一 BootstrapData"]
        O2["Δr_total = r128 − r8<br/>D_m = Δr_total^normal − Δr_total^C1<br/>I_MP = D_mamba − D_pythia"]
    end

    subgraph DG["诊断层（CPU·从缓存直算）"]
        P1["m4s_lag1_diagnostic.py<br/>相邻 TR 表示 cos，查特征平滑度"]
        P2["m4s_ood_diagnostic.py<br/>L2 范数 + participation ratio"]
    end

    subgraph FIG["Step 6 图表"]
        Q1["m6s_context_control_figure.py<br/>Figure 17 + Table 9"]
        Q2["m6s_degradation_figure.py<br/>Figure 18 + Table 10"]
    end

    R["结论：控制设计本身有 H 依赖的 OOD 混淆<br/>D_m/I_MP 不能干净归因于语言结构<br/>主实验确认性家族不受影响"]

    K1 --> K2 --> K3
    K4 -.设计约束.-> K2
    K3 --> M1 --> M2 --> N1 --> N2
    N2 --> O1 --> O2 --> Q1
    M2 --> P1 & P2
    P1 & P2 --> Q2
    Q1 & Q2 --> R
```

完整结果数字见 [实验补充/结果总记录.md](../实验补充/结果总记录.md)（权威记录）。

---

## 图 6 — 本机 / 服务器职责划分（算力约束）

```mermaid
flowchart LR
    subgraph LOCAL["本机（WSL2·无 GPU）"]
        LA["M0 冻结"]
        LB["M2 voxel_mask / ROI"]
        LC["M5 统计（纯 CPU bootstrap）"]
        LD["M6 全部绘图制表"]
        LE["写代码 / 单测 / 断言脚本"]
    end

    subgraph SRV["AutoDL 服务器（GPU）"]
        SA["M1 特征提取（模型前向）"]
        SB["M2-C eng1000 ridge"]
        SC["M3 竖切"]
        SD["M4 全矩阵 ridge（最重）"]
        SE["M4S Step3/4"]
    end

    GH["GitHub（mine remote）<br/>代码单向中转"]
    SCP["scp / ssh<br/>结果反向下载"]

    LE -->|git push| GH -->|git pull| SRV
    SRV -->|结果产物| SCP --> LC
    SCP --> LD
```

**约束**：任何模型推理 / ridge 拟合都不在本机跑，且需明确确认后才启动。
每次 push 后必须完成两步——本地确认推送成功 + 服务器 `git pull` 后再重跑，不能假定服务器已是最新代码。

---

## 与论文 Figure 1 的关系（别搞混）

项目里**已经有一张流程图**：`figures/{被试}/fig1_pipeline.png`，由
[scripts/m6_figures.py](../scripts/m6_figures.py) 的 `fig1()` 生成（v2 脚本不含 fig1，只产 fig2–fig5）。
两者视角不同，互补而非重复：

| | 论文 Figure 1 (`fig1_pipeline`) | 本文档 |
|---|---|---|
| 读者 | 论文审阅人 / 答辩老师 | 自己、接手代码的人 |
| 内容 | 7 步**方法**管线：词索引 → 特征 → Lanczos+FIR → 训练折内变换 → RidgeCV → story 级评分 → 3 折汇总 | 里程碑编排、脚本名、冻结产物、算力位置、统计口径分层 |
| 语言 / 形式 | 英文、matplotlib 竖排单链 | 中文、mermaid 多视图 |
| 是否含里程碑 | 不含（论文不谈 M0-M6） | 含 |

本文档的**图 2** 是 Figure 1 的展开版——同一条管线，但补上了防泄漏的参数复用关系、
评分 mask 的三重交集、以及 ROI 聚合顺序这些图里放不下的实现细节。答辩若被问
"你这条管线怎么保证没泄漏"，看图 2 比看 Figure 1 更容易答。

---

## 一句话总结运行顺序

```
M0 冻结 → M2 空间准备 →（M2-C 旁路验证管线）→ M1 特征提取
   → M3a/M3/M3b 竖切通关 → M4 全矩阵 108×3 次拟合
   → M5 配对 bootstrap + Holm → M6 图表交付
                                    ↘ M4S 补充控制（C1 支线，诊断性）
```
